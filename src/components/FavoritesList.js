// will iterate through array of user's favorite songs and renders each Favorite
import React from 'react';

const FavoritesList = () => {
  return <h3>FavoritesList</h3>;
};

export default FavoritesList;
